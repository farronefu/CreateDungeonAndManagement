# 開発上の指示

- 明示的な指示がない限りサブエージェントを使用しない。
- Godot 4.7.2 / GDScriptのプロジェクト。
- 操作はコントローラーだけで完結させる。指定の十字・X・LBを維持する。
- 生態系・戦闘の計算は `simulation.gd`、画面・入力は `game.gd` に分ける。
- 準備・戦闘以外のフェーズでは世界を進めない。強化・次ステージ移行でも停止する。
- 不明な原作仕様は独自仕様とし、原作の画像・音声・固有の台詞は取り込まない。
- 改修後は適切なテストを行い、変更範囲に応じて `tests/run_tests.gd` と `tests/ui_smoke.gd` を実行する。
