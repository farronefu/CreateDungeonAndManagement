# 初回1面の検証記録

## v0.5.1 魔物の表示不具合（2026-09-14）

- 原因：左向き描画で画像領域の描画先を負の幅にしていたため、Compatibility描画で画像がずれたり消えたりしていました。足元を中心とする描画変換で反転し、描画先の幅を正の値に維持するよう修正。
- 修正前は左右同位置の画像比較で灯苔が488→0ピクセル、虫744→124、石角832→200、卵424→32と欠落。修正後は4種類すべて左右同数で表示。
- `tests/sprite_visibility.gd` は実レンダラー必須（headless不可）。画面x=64/320/640/896の4か所×4種類、計16項目成功。右側の戦闘画面も `images/v051/right-combat.png` に保存して確認。
- 既存のロジック54、勇者48、描画設定13、合成Joypadの画面遷移14項目もすべて成功。新規検証と合わせて145項目成功。
- 生態系・戦闘判定・勇者の表示サイズは変更していません。

## v0.5 新勇者・整理の検証（2026-09-14）

- ロジック54、描画設定13、勇者48、合成Joypadによる画面遷移14項目、計129項目成功・失敗0。
- 新勇者の歩行シート・ポートレート・仮モーションを確認。実画面8枚を `images/v05/` に保存。
- 旧PNG9枚と未使用処理を削除。詳細は [整理記録](cleanup-v0.5.md)。
- 物理コントローラーでの操作は未検証。専用攻撃・見回し・喜び・死亡シートは追加待ち。
- ログ保存・シェーダーキャッシュ・証明書ストアの環境警告は継続。GDScriptの解析・実行エラーはなし。

## v0.2 画面改修の検証（2026-09-13）

- ロジック・入力・カメラ境界：54項目成功。画面端のマスとHUDの非重複、地上の非操作領域、72段目へのスクロールを追加確認。
- Joypad入力からの画面遷移：14項目成功。実行画面の画像を更新。
- Windows x86_64 releaseを書き出し、単体EXEの起動を確認。
- `simulation.gd`は変更なし。生態系・戦闘・資源計算は維持。
- 地上風景は固定の装飾領域。地下のカメラとは別に表示する仕様。
- 物理コントローラー操作は引き続き未検証。動作別アニメーションは未実装で、4コマの基本アニメーション。
- 開発サンドボックスのGodotログ保存・シェーダーキャッシュ・証明書ストアの警告は継続。GDScriptの解析・実行エラーはなし。
- 高速なheadlessテストでは音声ミキサーが進行しないため、ロジックテスト内だけ音声を無効化。実画面テストと製品側の音声は維持。

2026-09-08 / Godot 4.7.2 stable / Windows / Compatibility (OpenGL 3.3)

## 結果

- `tests/run_tests.gd`：49項目、失敗0。
- `tests/ui_smoke.gd`：14項目、失敗0。JoypadイベントをGodotの実際の入力経路へ流して確認。
- Windows x86_64 release：エクスポート成功。生成した単体実行ファイルの起動を確認。
- タイトル・育成・配置・侵入・ポーズ・勝利・敗北の画面を出力。日本語表示を確認し、マップに重なる案内をマップ外へ修正。

## 確認した挙動

- 60×40、60×72のマップ生成と固定シード。
- 掘削の境界・隣接条件・資源消費・空マスの重複消費防止。
- 養分吸収と放出、捕食による資源移動、死亡時の土への還元、卵の孵化。
- 勇者2人、MPを消費する回復、捕縛、出口への移動、救出、再捕縛、運搬中のスキル禁止。
- 全員撃破による勝利、運搬者の出口到達による敗北、報酬の二重加算防止。
- タイトル・到来・配置・ポーズ・強化・次ステージ移行・勝利・敗北中の世界停止。
- 準備時間経過、Yによる早期到来、Aによる配置・開始・リトライ。
- X、十字キー、長押しリピート、Xとの同時操作、LB交互切替、運搬者優先、カーソルとカメラの分離。
- コントローラー切断通知でポーズ。

## 動作と性能

固定シードの掘削・育成・侵入を通したシミュレーションは勝利まで到達。試験用の掘削パターンでは戦闘7.1秒で終了し、初回のバランスは易しめです。

負荷確認：60×72マスを開放、初期220体、勇者2人、1,000更新。CPU時間合計360.65ms、1更新の最大6.19ms。この試験中は捕食・死亡によって個体数が減少します。220体が常時維持される最悪条件や描画を含むFPS保証ではありません。

## 検証の限界

- 物理コントローラーのボタンを押す実機検証は未実施。合成Joypadイベントとゲーム内部の入力マッピングで検証しています。
- 原作との画質・速度・数値の一致を測定したものではありません。素材は独自の初期案です。
- 開発サンドボックスではGodotのAppDataログ・シェーダーキャッシュ保存とシステム証明書ストア読み込みに環境由来のエラーが出ます。GDScriptの解析・実行エラーはありません。単体releaseのheadless起動ではこれらの出力はありません。
- この試作は保存機能やネットワークを使用しません。Steam SDK、永続セーブ、複数ステージは未実装です。
- 強化・次ステージ移行は停止状態として試験していますが、該当画面自体はまだありません。
- バランス・対応コントローラー・異なるPC環境での遊びやすさは、プレイ確認後に調整します。

## 再実行

READMEの検証コマンドを使用。性能試験のみ実行する場合：

```powershell
godot --headless --path . --script res://tests/stress.gd
```

## v0.5 Windows配布確認

Windows releaseのエクスポート成功。配布EXEをheadlessで起動し、正常終了（終了コード0）を確認。対話画面の確認は上記のソース実行時キャプチャで行っています。

## v0.5.2 マントなし勇者（2026-09-14）

320×160・40×40セルの新しい移動シートと正面アイコンを反映。4方向8コマ・8fps・1マス1秒は維持。足元は20,38、倍率1.25（体の高さ約40〜44px）。専用攻撃等は仮ポーズのまま。ロジック54・勇者48・描画設定13・入力画面遷移14項目が成功。画面記録はimages/v052。
